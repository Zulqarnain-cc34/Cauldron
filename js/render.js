/**
 * World rendering — WebGL2 grid (see render-gl.js). Overlay sprites use overlay.js.
 */
export { createWebGLRenderer, renderWorld } from './render-gl.js';
export { canvasPixelSize as canvasSize } from './world.js';
